# Use default linker on Linux (clang/mold not installed)
[target.x86_64-unknown-linux-gnu]
# linker = "clang"
# rustflags = ["-C", "link-arg=-fuse-ld=mold"]

[build]
# Parallel frontend for faster compilation
# Uncomment for nightly: rustflags = ["-Z", "threads=4"]
